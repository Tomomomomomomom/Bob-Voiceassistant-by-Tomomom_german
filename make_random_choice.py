import random

def make_random_choice(low, high):
  # Generate a random number between the specified range
  choice = random.randint(low, high)

  # Return the random number
  return choice

